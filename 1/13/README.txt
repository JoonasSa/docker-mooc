For the Dockerfile to work you need to clone https://github.com/docker-hy/spring-example-project to the same directory.

After building the image you can run the container by running: 'docker run -d -p 8080:8080 <id-of-image>'.
