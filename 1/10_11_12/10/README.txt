For the Dockerfile to work you need to clone https://github.com/docker-hy/frontend-example-docker to the same directory.

After building the image you can run the container by running: 'docker run -d -p 5000:5000 <id-of-image>'.
