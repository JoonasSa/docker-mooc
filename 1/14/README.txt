For the Dockerfile to work you need to clone https://github.com/docker-hy/rails-example-project to the same directory.

After building the image you can run the container by running: 'docker run -d -p 3000:3000 <id-of-image>'.
